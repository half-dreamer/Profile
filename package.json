{
  "name": "personal-page-mpa",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run src",
    "test:e2e:setup": "playwright install chromium",
    "test:e2e": "npm run test:e2e:setup && playwright test tests/e2e/smoke.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "playwright": "^1.59.1",
    "vite": "^7.1.0",
    "vitest": "^3.2.4",
    "wait-on": "^9.0.5"
  },
  "dependencies": {
    "zod": "^4.4.1"
  }
}
